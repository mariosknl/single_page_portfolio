# Use strict-peer-dependencies=false to avoid peer dependency issues
strict-peer-dependencies=false

# Use node-linker=hoisted for better performance and compatibility
node-linker=hoisted

# Enable automatic installation of peer dependencies
auto-install-peers=true

# Save exact versions by default
save-exact=true 